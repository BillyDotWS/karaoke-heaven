premium.add = async (user, days) => {

    // add days to user

    // add role if needed

}

premium.remove = async (user, days) => {

    // remove days from user

    // remove role if needed

}

premium.set = async (user, days) => {

    // set days of user

    // remove role if needed

    // add role if needed

}