
module.exports = {
	name: 'cancelevent',
	description: 'Cancel an event',
	requiredScope: ['cancelevent'],
	async execute(event, req, args) {

        // ensure event exists

        // delete event from db

        // delete embed

        // return with raw json value

    }

}