
module.exports = {
	name: 'transactions',
	description: 'Manipulate balance',
	requiredScope: ['transactions'],
	async execute(user, type, amount, req) {

        // fetch user's balance

        // check type (withdraw, set, add)

        // do x based on type

        // return with raw json value

    }

}