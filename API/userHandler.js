
module.exports = {
	name: 'usermodify',
	description: 'Modify user',
	requiredScope: ['usermodify'],
	async execute(user, req, args) {

        // edit to database

        // check changes, run specific changes if needed

        // return with raw json value

    }

}