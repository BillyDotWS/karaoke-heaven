const currency = {};

currency.remove = async (user, amount) => {

    // check user has enough

    // withdraw amount

    // return balance json

}

currency.fetch = async (user) => {

    // check user balance

    // return balance json

}

currency.add = async (user, amount) => {

    // add amount to user

    // return balance json

}

currency.set = async (user, amount) => {

    // set amount to user

    // return balance json

}
