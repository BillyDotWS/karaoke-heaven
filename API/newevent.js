
module.exports = {
	name: 'newevent',
	description: 'Create new event',
	requiredScope: [],
	async execute(req, args) {

        // validation checks

        // if all good, add to database

        // announce the event in discord

        // return with raw json value

    }

}