
module.exports = {
	name: 'premium',
	description: 'Manage premium status',
	requiredScope: ['premium'],
	async execute(user, type, time, req) {

        // fetch user's premium status

        // check type (remove, set, add)

        // do x based on type

        // return with raw json value

    }

}