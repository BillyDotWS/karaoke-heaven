
module.exports = {
	name: 'users',
	description: 'Fetch users',
	requiredScope: ['users'],
	async execute(user, req) {

        // fetch user


        // if it's a web request, ignore email


        // if not, return with raw json value

    }

}