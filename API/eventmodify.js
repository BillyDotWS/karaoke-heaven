
module.exports = {
	name: 'eventmodify',
	description: 'Modify an event',
	requiredScope: ['eventmodify'],
	async execute(event, req, args) {

        // ensure event exists

        // edit to database

        // check changes, edit embed if needed

        // return with raw json value

    }

}